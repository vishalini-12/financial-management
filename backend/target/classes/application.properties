
spring.application.name=${APP_NAME:FinalProjectFinance}

# Server
server.port=${PORT:8080}

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,https://financial-management-gjw5vuj4l-vishs-projects-b9b54481.vercel.app,https://financial-management-five.vercel.app,https://financial-management-80wmoiskx-vishs-projects-b9b54481.vercel.app,https://financial-management-8lshu466l-vishs-projects-b9b54481.vercel.app}

# Database (Local MySQL - fallback to Railway)
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/fin_management?createDatabaseIfNotExist=true}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Vish@1213}

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.sql.init.mode=${SQL_INIT_MODE:always}

# Disable H2
spring.h2.console.enabled=false

# JWT
jwt.secret=${JWT_SECRET:RZwIqkcRGCAJQQKjwnlGpWQJw5+SlgaKG+5GHbcrHHI=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# File upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB





# spring.application.name=${APP_NAME:FinalProjectFinance}

# # CORS Configuration
# cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,https://*.vercel.app,https://financial-ledger-frontend.vercel.app,https://financial-management-five.vercel.app}

# # Database Configuration - Using MySQL
# spring.datasource.url=${DATABASE_URL:jdbc:mysql://mysql.railway.internal:3306/railway}
# spring.datasource.driverClassName=${DB_DRIVER:com.mysql.cj.jdbc.Driver}
# spring.datasource.username=${DB_USERNAME:root}
# spring.datasource.password=${DB_PASSWORD:EwAyyxqGarzrrxUeAeflEPAiISoPLHos}
# spring.jpa.database-platform=${JPA_PLATFORM:org.hibernate.dialect.MySQLDialect}
# spring.jpa.show-sql=${JPA_SHOW_SQL:true}
# spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
# spring.sql.init.mode=${SQL_INIT_MODE:always}

# # JWT Configuration
# jwt.secret=${JWT_SECRET:mySuperSecretKeyThatIsAtLeast32CharactersLong}
# jwt.expiration=${JWT_EXPIRATION:86400000}

# # Server Configuration
# server.port=${PORT:8080}

# # File Upload Configuration
# spring.servlet.multipart.enabled=${MULTIPART_ENABLED:true}
# spring.servlet.multipart.file-size-threshold=${MULTIPART_THRESHOLD:2KB}
# spring.servlet.multipart.max-file-size=${MULTIPART_MAX_FILE_SIZE:10MB}
# spring.servlet.multipart.max-request-size=${MULTIPART_MAX_REQUEST_SIZE:10MB}

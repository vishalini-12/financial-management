# Database Configuration (Railway Environment Variables)
# Railway automatically provides DATABASE_URL, or you can construct it from:
# DATABASE_URL=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQL_DATABASE}?useSSL=true&serverTimezone=UTC
# DB_USERNAME=${MYSQLUSER}
# DB_PASSWORD=${MYSQL_ROOT_PASSWORD}

# For Railway deployment, use these (Railway provides DATABASE_URL automatically):
# DATABASE_URL (provided by Railway)
# DB_USERNAME (provided by Railway)
# DB_PASSWORD (provided by Railway)

# JWT Configuration
JWT_SECRET=mySuperSecretKeyThatIsAtLeast32CharactersLong
JWT_EXPIRATION=86400000

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Server Configuration
SERVER_PORT=8080
